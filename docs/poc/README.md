# PoC (Proof of Concept)

기획·아이디어 검증용 정적 HTML. 운영 코드와 무관, 참고 자료.

| 파일 | 의도 |
|---|---|
| `poc-project-pipeline.html` | 6 step 파이프라인 UI 흐름 PoC. 실제 구현은 `src/app/(dashboard)/projects/[id]/`. |
| `poc-recruit-landing.html` | 모집 랜딩 페이지 PoC. 실제 구현은 `recruit-landing/`. |

→ 운영 코드 작업 시 참조 가능. 실제 사용처 없음.
